[project]
name = "forge-tool-pack-scope"
version = "0.1.0"
description = "Package a Python local lab for pack analysis with transition tables, invalid-transition tests, and documented operating limits."
requires-python = ">=3.11"
